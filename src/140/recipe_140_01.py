# 절댓값 계산
val = -100
abs_val = abs(val)
print("절댓값", abs_val)

# 합계, 최댓값, 최솟값 계산
val_list = [100, 76, 5, -9, 25, 3.5, -0.99]
sum_val = sum(val_list)
max_val = max(val_list)
min_val = min(val_list)

print("합계", sum_val)
print("최댓값", max_val)
print("최솟값", min_val)
